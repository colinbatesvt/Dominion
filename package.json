{
  "name": "dominion-server",
  "version": "1.0.0",
  "description": "server for the dominion web app game",  
  "engines": {
    "node": "12.x"
  },
  "main": "dist/server.js",
    "scripts": {
	  "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
	  "postinstall": "cd Common && npm run tsc && cd ../Webapp && ng build -prod && cd .. && npm run tsc",
    "start": "node dist/src/server.js"
  },
  "author": "Colin Bates",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.6",
    "tslint": "^6.1.2",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "angular": "^1.7.9",
    "async": "^3.2.0",
    "express": "^4.17.1",
    "socket.io": "^2.3.0",
    "@angular/cli": "~9.1.6"
  }
}
